apiVersion: v1
kind: Service
metadata:
  name: banco-digital-backend-nodeport
  namespace: banco-digital
spec:
  selector:
    app: banco-digital-backend
  ports:
    - name: http
      port: 8080
      targetPort: 8080
      nodePort: 30080
  type: NodePort
